esults", exist_ok=True)

    # # Save benchmark results to JSON file
    # with open(os.path.join("Results", "results_avl.json"), "w") as f:
    #     json.dump(results, f, indent=2)
    # print("\nResults saved to